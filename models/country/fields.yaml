# ===================================
#  Form Field Definitions
# ===================================

fields:

    name:
        label: Name
        span: auto
        default: Australia

    code:
        label: Code
        span: auto
        default: AU
        attributes:
            data-input-preset: 'input[name="Country[name]"]'
            data-input-preset-type: slug
            data-input-preset-closest-parent: form

    pages:
        label: Pages
        type: checkboxlist