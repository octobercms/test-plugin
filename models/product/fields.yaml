# ===================================
#  Form Field Definitions
# ===================================

fields:
    title:
        label: Product Name
        required: true

    price:
        label: Price (Propagated)
        type: number

    description:
        label: Description

    categories:
        label: Category
        type: relation

    certificate:
        label: Certificate
        type: fileupload

    gallery:
        label: Gallery
        type: fileupload

