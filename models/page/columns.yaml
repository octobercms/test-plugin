columns:
    id:
        label: ID
        type: number

    type:
        label: Type
        type: selectable
        options:
            1: Simple
            2: Complex
            # @todo available in v2.2+
            # 1: [Simple, 'icon-clock-o']
            # 2: [Complex, 'icon-exclamation']

    content[title]:
        label: content
        type: text
