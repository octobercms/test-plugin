name: Docker Image CI

on:
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build the Docker image
      run: docker build --tag docker.pkg.github.com/$GITHUB_REPOSITORY/oc-test-plugin:$GITHUB_RUN_ID .
